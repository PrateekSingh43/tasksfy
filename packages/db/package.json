{
  "name": "@repo/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "db:generate": "dotenv -e .env.cli -- prisma generate",
    "db:push": "dotenv -e .env.cli -- prisma db push",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc",
    "check-types": "tsc --noEmit"
  },
  "dependencies": {
    "@prisma/client": "6.18.0"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^20.14.10",
    "dotenv-cli": "^7.4.2",
    "eslint": "^9.8.0",
    "prisma": "6.18.0",
    "typescript": "^5.5.3"
  }
}