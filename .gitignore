# --------------------
# GLOBAL IGNORES
# --------------------

# Environment variables (MUST be ignored)
.env
.env.cli
.env.development
.env.production
.env.local
.env.*.local
.env.*

# Keep example files
# The double exclamation mark (!!) is not needed if the original .env* is specific.
# Since we use `.env.*`, we must use `!` to un-ignore the example.
!.env.example

# Node.js
node_modules
pnpm-lock.yaml

# Build & Tool Outputs
dist/
build/
out/
.next/ # Next.js (Even if not used, keeps the config clean)
.turbo/
.cache-loader/
.docusaurus/ # Docusaurus build cache

# TypeScript
*.tsbuildinfo

# Test Artifacts
coverage/

# OS and IDE Files
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
# Temporary files
*.tmp
*.temp
*.swp
*.swo	
# Dependency directories
bower_components/
jspm_packages/
# Generated files
*.lock
*.log
*.cache/
*.bak
# Miscellaneous
*.orig
*.rej
*.old
*.backup
*.~*